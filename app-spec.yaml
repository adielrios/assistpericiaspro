name: assistpericias-adiel
region: nyc
services:
- name: web
  environment_slug: python
  instance_size_slug: basic-xxs
  instance_count: 1
  source_dir: /
  http_port: 8080
  build_command: pip install -r requirements.txt
  run_command: gunicorn app:app
  envs:
  - key: PORT
    value: "8080"
  - key: FLASK_ENV
    value: "production"
  - key: API_KEY
    value: "c018d16a8e2b4581b8f18cab39f37918"
    scope: RUN_TIME
    type: SECRET
